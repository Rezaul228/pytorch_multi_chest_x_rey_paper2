# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.ipynb_checkpoints/

# Environments
.venv/
venv/
env/

# Data / model artifacts
*.pt
*.pth
*.ckpt
*.h5
/data/
datasets/
checkpoints/
outputs/
logs/
wandb/
*.log

# OS / editor
.DS_Store
.vscode/
*.swp

# Per-patient MIMIC-CXR derived labels (PhysioNet DUA: credentialed-access only, not for public redistribution)
test_labels_chexpert.csv
test_labels_chexpert_binary.csv
test_labels_negbio.csv
hard_negative_tags_test_paper2.csv
section_boundaries_train_paper2.csv
section_boundaries_val_paper2.csv
section_boundaries_test_paper2.csv

# Per-query result files keyed by raw MIMIC-CXR study_id (PhysioNet DUA: the
# identifier itself is restricted, even without images/report text attached).
# Purged from git history on 2026-09-15; keep these local-only from now on.
divergence_scores_test_paper2.csv
per_query_i2t_metrics_seed_*.csv

# Same caution extended to ReXGradient and Open-I: any per-study-linked file
# (study_id column) or file containing raw report text, across all three
# datasets' data/ and results/ subfolders.
divergence_scores_test_*.csv
test_labels_chexbert.csv
test_labels_chexbert_binary.csv
chexbert_input_test*.csv
chexbert_out/
# Diagnostic C: per-query cache under CheXbert labels (study_id-keyed, MIMIC DUA)
per_query_i2t_metrics_*.csv
# Step A2: top-10 whole Impressions per dataset (raw report text)
impression_top10_*.csv
# Open-I per-query caches (study_id-keyed; same caution as MIMIC/ReXGradient)
per_query_metrics_*.csv
# MIMIC data-scale ablation id lists (study_id-keyed, DUA)
mimic/data/scale_ablation/
